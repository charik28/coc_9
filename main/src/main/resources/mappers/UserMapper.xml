<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dz.afaris.mappers.UserMapper">

    <!-- Reusable result mapping -->
    <resultMap id="UserDtoMap" type="dz.generator.service.dto.UserDto">
        <id property="id" column="id" />
        <result property="login" column="login" />
        <result property="email" column="email" />
        <result property="code" column="code" />
    </resultMap>

    <!-- findAll -->
    <select id="findAll" resultMap="UserDtoMap">
        SELECT id, login, email,code
        FROM jhi_user
    </select>

    <!-- findById -->
    <select id="findById" parameterType="long" resultMap="UserDtoMap">
        SELECT id, login, email,code
        FROM jhi_user
        WHERE id = #{id}
    </select>

    <!-- findById -->
    <select id="findByLogin" parameterType="java.lang.String" resultMap="UserDtoMap">
        SELECT id, login, email,code
        FROM jhi_user
        WHERE login = #{login}
    </select>

</mapper>
